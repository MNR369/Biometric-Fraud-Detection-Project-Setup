[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "biometric-fraud-detection"
version = "1.0.0"
description = "Deterministic, dependency-free biometric fraud-risk baseline"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
